// ltl-matrix/main.ltl
//
// Dense matrix algebra — multiply, invert, determinant.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-matrix v0.1.0")
    io::println("Dense matrix algebra — multiply, invert, determinant.")
    0
}
