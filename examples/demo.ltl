// examples/demo.ltl — runnable demo for ltl_matrix

import std::io
use ltl_matrix as lib

fn main() {
    io::println("ltl_matrix demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}